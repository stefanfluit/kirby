---
- name: Test - one task changed, not tested
  hosts: localhost
  gather_facts: false
  vars:
    var1: v1
  tasks:
    - name: Render unchecked template
      ansible.builtin.template:
        src: dummy.conf.j2
        dest: ./dummy_no_serverspec_test.conf
        mode: "0644"
