[tool.pytest.ini_options]
testpaths = ["tests/unit"]

[tool.ruff]
target-version = "py310"
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP"]
per-file-ignores = { "tests/features/steps/steps.py" = ["F811"] }

[tool.ruff.lint.isort]
known-first-party = []
